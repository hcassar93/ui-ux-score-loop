interface:
  display_name: "UI/UX Score Loop"
  short_description: "Browser UX scoring loop with dashboard"
  default_prompt: "Use $ui-ux-score-loop to improve one product flow from the real user entry point with browser screenshots, responsive/theme scores, and a delivered dashboard."
